apiVersion: v1
kind: Pod
metadata:
  name: bash-pod2a
  annotations:
    description: "Test4"
spec:
  containers:
  - name: bash-container3
    image: docker.io/bash:latest
    command: ["sleep"]
    args: ["infinity"]
    env:
    - name: MY_ENV_VAR
      valueFrom:
        configMapKeyRef:
          name: configmap1
          key: MY_ENV_VAR

apiVersion: v1
kind: Pod
metadata:
  name: bash-pod2a
  annotations:
    description: "Test1"
spec:
  containers:
  - name: bash-container2
    image: docker.io/bash:latest
    command: ["sleep"]
    args: ["infinity"]
    env:
    - name: MY_ENV_VAR
      valueFrom:
        configMapKeyRef:
          name: configmap1
          key: MY_ENV_VAR
